{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(gdb) Launch (WSL)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/.vscode/bin/app",
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "preLaunchTask": "build (WSL)",
      "externalConsole": false
    }
  ]
}
